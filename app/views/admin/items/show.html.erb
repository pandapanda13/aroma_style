<div class="container">
 <div class="row my-2">
  <p class="title col-2 offset-1 px-4">商品詳細</p>
 </div>
 <div class="row mt-5">
  <div class="col-5">
    <%= image_tag @item.image_id, size: "300x240", class: "rounded-circle"%>
  </div>

  <div class="col-2">
    <p class="">商品名</p>
    <p class="">容量</p>
    <p class="">商品説明</p>
    <p class="">税込価格（税抜価格）</p>

   <%= link_to "編集する", edit_admin_item_path(@item.id), class: 'btn btn-outline-success ' %>
  </div>
  <div class="col-4">
    <p><%= @item.name %></p>
    <p><%= @item.volume %></p>
    <p><%= @item.introduction %></p>
    <p><%= @item.add_tax_price.to_s (:delimited) %>
    (
    <%= @item.price.to_s (:delimited) %>)円

  </div>
 </div>
</div>
